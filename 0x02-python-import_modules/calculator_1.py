#!/usr/bin/python3
def add(a, b):
    Args:
        a: first integer
        b: second ineger
        return (a + b)
    def sub(a, b):
        Args:
            a: first integer
            b: second integer
            Returns:
                The return value. a - b
                return (a - b)
            def mul(a, b):
                Args:
                    a: first integer
                    b: second integer
                    return (a * b)
                def div(a, b):
                    Args:
                        a: first integer
                        b: second intege
                        return int(a / b)
